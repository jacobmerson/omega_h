name: Perlmutter
on:
  push:

jobs:
  build:
    runs-on: self-hosted

    steps:

    - name: checkout omega_h
      uses: actions/checkout@v4
      with:
        path: omega_h

    - name: load modules
      run: |
          module load gcc
          module load python

    - name: print dir
      working-directory: omega_h/.github/workflows
      run: ls

    